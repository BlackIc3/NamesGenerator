{
  "name": "name_generator",
  "type": "commonjs",
  "version": "1.0.0",
  "description": "Name Generator for OpenMaps POIs",
  "main": "app.js",
  "scripts": {
    "start": "tsc --build tsconfig.json && node build/app.js",
    "validate": "tsc --build tsconfig.json && node build/app.js validate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "xml-stream": "^0.4.5"
  },
  "devDependencies": {
    "@types/node": "^14.14.32"
  }
}
